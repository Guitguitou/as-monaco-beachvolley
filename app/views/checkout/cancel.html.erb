<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="bg-white shadow-lg rounded-lg p-8 text-center">
    <%# Cancel icon %>
    <div class="mb-6">
      <div class="w-20 h-20 bg-orange-100 rounded-full flex items-center justify-center mx-auto">
        <svg class="w-12 h-12 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </div>
    </div>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">
      Paiement annulé
    </h1>

    <p class="text-lg text-gray-700 mb-6">
      Votre paiement a été annulé. Aucun débit n'a été effectué.
    </p>

    <% if @credit_purchase %>
      <p class="text-sm text-gray-500 mb-6">
        Référence : <span class="font-mono"><%= @credit_purchase.sherlock_transaction_reference %></span>
      </p>
    <% end %>

    <div class="space-y-3">
      <%= link_to "Réessayer", 
          admin_payments_path, 
          class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200" %>
      
      <%= link_to "Retour à l'accueil", 
          root_path, 
          class: "inline-block bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-3 px-6 rounded-lg transition duration-200" %>
    </div>
  </div>
</div>

