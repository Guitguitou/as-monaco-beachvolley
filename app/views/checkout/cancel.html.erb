<div class="max-w-2xl mx-auto">
  <div class="bg-white rounded-none border border-gray-200 shadow-lg overflow-hidden">
    <%# Cancel header with gradient %>
    <div class="bg-gradient-to-r from-asmbv-red to-asmbv-red-dark p-8 text-white text-center">
      <p class="text-xs uppercase tracking-widest text-white/75 font-semibold mb-3">Boutique</p>
      <div class="flex justify-center mb-4">
        <div class="p-4 bg-white/20 rounded-none">
          <%= lucide_icon("x-circle", size: 64) %>
        </div>
      </div>
      <h1 class="font-bebas-neue text-5xl tracking-wider uppercase mb-2">Paiement annulé</h1>
      <p class="text-base opacity-90">Aucun débit n'a été effectué.</p>
    </div>

    <%# Content %>
    <div class="p-8">
      <div class="text-center mb-6">
        <p class="text-gray-600 mb-2">Aucun débit n'a été effectué sur votre compte.</p>
        <% if @credit_purchase %>
          <p class="text-sm text-gray-500">
            Référence : <span class="font-mono"><%= @credit_purchase.sherlock_transaction_reference %></span>
          </p>
        <% end %>
      </div>

      <%# Info box %>
      <div class="bg-gray-50 border border-gray-200 rounded-none p-4 mb-6">
        <div class="flex gap-3">
          <%= lucide_icon("info", size: 20, class: "text-gray-700 flex-shrink-0 mt-0.5") %>
          <div class="text-sm text-gray-700">
            <p class="font-semibold mb-1">Vous avez changé d'avis ?</p>
            <p>Vous pouvez réessayer à tout moment en retournant sur la page des packs.</p>
          </div>
        </div>
      </div>

      <%# Actions %>
      <div class="flex flex-col sm:flex-row gap-3">
        <%= link_to packs_path,
              class: "flex-1 inline-flex items-center justify-center gap-2 rounded-none bg-asmbv-red px-6 py-3 text-base font-semibold text-white hover:bg-asmbv-red-dark transition-all" do %>
          <%= lucide_icon("package", size: 20) %>
          Voir les packs
        <% end %>
        
        <%= link_to root_path,
              class: "flex-1 inline-flex items-center justify-center gap-2 rounded-none bg-gray-200 px-6 py-3 text-base font-semibold text-gray-800 hover:bg-gray-300 transition-all" do %>
          <%= lucide_icon("home", size: 20) %>
          Retour à l'accueil
        <% end %>
      </div>
    </div>
  </div>
</div>
