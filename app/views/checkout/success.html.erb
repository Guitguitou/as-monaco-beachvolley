<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="bg-white shadow-lg rounded-lg p-8 text-center">
    <%# Success icon %>
    <div class="mb-6">
      <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto">
        <svg class="w-12 h-12 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
      </div>
    </div>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">
      Paiement réussi ! 🎉
    </h1>

    <% if @credit_purchase %>
      <div class="mb-6">
        <p class="text-lg text-gray-700 mb-2">
          <span class="font-semibold text-green-600"><%= @credit_purchase.credits %> crédits</span> 
          ont été ajoutés à votre compte
        </p>
        <p class="text-sm text-gray-500">
          Référence : <span class="font-mono"><%= @credit_purchase.sherlock_transaction_reference %></span>
        </p>
      </div>

      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
        <p class="text-sm text-blue-800">
          <strong>Nouveau solde :</strong> 
          <%= current_user.balance&.amount || 0 %> crédits
        </p>
      </div>
    <% else %>
      <p class="text-gray-600 mb-6">
        Votre paiement a été traité avec succès.
      </p>
    <% end %>

    <div class="space-y-3">
      <%= link_to "Retour à mes paiements", 
          admin_payments_path, 
          class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200" %>
      
      <%= link_to "Voir mes séances", 
          sessions_path, 
          class: "inline-block bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-3 px-6 rounded-lg transition duration-200" %>
    </div>
  </div>
</div>

