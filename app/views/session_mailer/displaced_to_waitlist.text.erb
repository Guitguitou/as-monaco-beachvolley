Bonjour <%= @user.first_name.presence || @user.email %>.

Un joueur d'un groupe prioritaire vient de s'inscrire, tu repasses donc en liste d'attente.

Session <%= @session_name %> du <%= @session_date %> à <%= @session_time %>. Tes crédits t'ont été recrédités. Si une place se libère, tu seras automatiquement repositionné selon ta priorité.

Voir la session : <%= @session_url %>

À bientôt sur le sable,
L'équipe AS Monaco Beach Volley
