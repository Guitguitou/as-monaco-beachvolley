<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "AS Monaco Beach Volley App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag "logo.png", type: "image/png" %>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: {
              'asmbv-red': '#e21e22',
              'asmbv-red-light': '#fef2f2',
              'asmbv-red-dark': '#b91c1c',
            }
          }
        }
      }
    </script>
    <%= javascript_importmap_tags %>
    <%= yield :head if content_for?(:head) %>
  </head>

  <body class="bg-white min-h-screen text-gray-900">
    <%= yield %>
    <%= yield :scripts if content_for?(:scripts) %>
  </body>
  <%= render "layouts/footer" %>
</html>
